import React, { PureComponent } from 'react';
import {
  FormContainer,
  FromTitle,
} from './styled';

class SignIn extends PureComponent {
  render() {
    return (
      <FormContainer>
        <FromTitle>
          SignIn
        </FromTitle>
      </FormContainer>
    );
  }
}

export default SignIn;
