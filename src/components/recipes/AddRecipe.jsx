import React from 'react';
import {
  MainAppContainer,
  FromTitle,
  Form,
  FormInput,
} from '../../styled';

const AddRecipe = () => (
  <MainAppContainer>
    <FromTitle>AddRecipe</FromTitle>
  </MainAppContainer>
);

export default AddRecipe;
